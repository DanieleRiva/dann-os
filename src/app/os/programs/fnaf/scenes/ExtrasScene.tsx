import { Engine } from "../core/Engine"

interface ExtrasSceneProps {
    engine: Engine
}

const ExtrasScene = ({ engine }: ExtrasSceneProps) => {
    return (
        <div>ExtrasScene</div>
    )
}

export default ExtrasScene