import { Engine } from '../core/Engine'

interface DeathSceneProps {
    engine: Engine
}

const DeathScene = ({ engine }: DeathSceneProps) => {
    return (
        <div>DeathScene</div>
    )
}

export default DeathScene